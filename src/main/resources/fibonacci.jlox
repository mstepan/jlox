// print first 10 fibonacci numbers

print "Calculating and printing first 10 fibonacci numbers";

fun fib(n){
	if( n <= 1 ){
		return 1;
	}
	return fib(n-1) + fib(n-2);
}

fun main(){
	var i;
	for( i = 0; i < 10; i = i + 1 ){
		print "fib(" + i +") = " + fib(i);
	}
}

main();
